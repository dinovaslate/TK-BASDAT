substitutions:
  _IMAGE: asia-southeast2-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/tk-basdat-aeromiles-api:latest

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - ${_IMAGE}
      - backend

images:
  - ${_IMAGE}
