SECRET_KEY=change-this-for-your-local-machine
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=

DB_NAME=aeromiles_db
DB_USER=postgres
DB_PASSWORD=your_postgres_password
# DB_PASS is also accepted when DB_PASSWORD is not set.
DB_HOST=localhost
DB_PORT=5432
DB_SCHEMA=aeromiles
# Set this on Cloud Run to use the Cloud SQL Unix socket:
# INSTANCE_CONNECTION_NAME=project-id:region:instance-name

CORS_ALLOWED_ORIGINS=http://localhost:3000
